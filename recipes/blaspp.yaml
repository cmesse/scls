name: blaspp
version: 2025.05.28
summary: C++ API for the Basic Linear Algebra Subprograms (BLAS)
homepage: https://icl.utk.edu/slate/
license: BSD-3-Clause

source:
  url: https://github.com/icl-utk-edu/blaspp/releases/download/v%{version}/blaspp-%{version}.tar.gz

patches:
  all:
    - blaspp_fix_testsweep.patch
    - blaspp_cblas_include.patch

requires:
  all:
    - cmake
    - testsweeper
  gcc:
    - openblas
  macos:
    - openblas
  debug:
    - lapack

features:
  fortran: false
  mpi: false
  openmp: false
  math: serial

configure:
  type: cmake
  pre:
    - "LD_LIBRARY_PATH=%{prefix}/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}; export LD_LIBRARY_PATH"
  post:
    - "unset LD_LIBRARY_PATH"
  args:
    - "-DCMAKE_BUILD_TYPE=Release"
    - "-DCMAKE_INSTALL_LIBDIR=lib"
    - "-DBUILD_SHARED_LIBS=ON"
    - "-DCMAKE_BUILD_RPATH=%{prefix}/lib"
    - "-Dbuild_tests=ON"
  flavor_args:
    gcc:
      - '-Dblas="OpenBLAS"'
      - '-Dlapack="generic"'
      - "-Dgpu_backend=none"
      - '-DBLAS_LIBRARIES=%{prefix}/lib/libopenblas.so'
      - '-DLAPACK_LIBRARIES=%{prefix}/lib/libopenblas.so'
    macos:
      - '-Dblas="OpenBLAS"'
      - '-Dlapack="generic"'
      - "-Dgpu_backend=none"
      - '-DBLAS_LIBRARIES=%{prefix}/lib/libopenblas.dylib'
      - '-DLAPACK_LIBRARIES=%{prefix}/lib/libopenblas.dylib'
    debug:
      - '-Dblas="generic"'
      - '-Dlapack="generic"'
      - "-Dgpu_backend=none"
      - '-DLAPACK_LIBRARIES=-llapack'
    mkl:
      # Let blaspp's own BLASFinder auto-detect MKL — do NOT pass explicit
      # LAPACK_LIBRARIES here, because our %{math_ldflags} (serial) produces
      # -lmkl_sequential while BLASFinder picks -lmkl_gnu_thread, and having
      # both threading layers NEEDED in the same binary is undefined behavior.
      - '-Dblas=Intel MKL'
      - '-Dlapack=Intel MKL'
      - "-Dgpu_backend=none"
    cuda:
      - '-Dblas=Intel MKL'
      - '-Dlapack=Intel MKL'
      - "-Dgpu_backend=cuda"
  # LP64/ILP64 integer interface
  lp64_args:
    - '-Dblas_int=int (LP64)'
  ilp64_args:
    - '-Dblas_int=int64_t (ILP64)'
  optimization:
    O_level: 3

build:
  parallel: true
  in_source: false  # Build in separate directory

test:
  commands:
    - "cd build/test && LD_LIBRARY_PATH=%{prefix}/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH} python3 run_tests.py --quick"

registry:
  cflags: "-I%{prefix}/include"
  ldflags: "-L%{prefix}/lib -lblaspp"
