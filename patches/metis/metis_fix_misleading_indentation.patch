--- a/GKlib/getopt.c
+++ b/GKlib/getopt.c
@@ -340,10 +340,11 @@ gk_getopt_internal (argc, argv, optstring, longopts, longind, long_only)
   gk_optarg = NULL;

   if (gk_optind == 0 || !gk_getopt_initialized) {
-    if (gk_optind == 0)
+    if (gk_optind == 0) {
       gk_optind = 1;	/* Don't scan ARGV[0], the program name.  */
-      optstring = gk_getopt_initialize (argc, argv, optstring);
-      gk_getopt_initialized = 1;
+    }
+    optstring = gk_getopt_initialize (argc, argv, optstring);
+    gk_getopt_initialized = 1;
     }

   /* Test whether ARGV[gk_optind] points to a non-option argument.
@@ -697,9 +698,10 @@ gk_getopt_internal (argc, argv, optstring, longopts, longind, long_only)
 	  else
 	    c = '?';
 	}
-	else
+	else {
 	  /* We already incremented `gk_optind' once; increment it again when taking next ARGV-elt as argument.  */
 	  gk_optarg = argv[gk_optind++];
+	}
 	  nextchar = NULL;
       }
     }
--- a/GKlib/csr.c
+++ b/GKlib/csr.c
@@ -1341,9 +1341,10 @@ void gk_csr_Normalize(gk_csr_t *mat, int what, int norm)
         if (sum > 0) {
   	if (norm == 2)
   	  sum=1.0/sqrt(sum);
-  	else if (norm == 1)
+  	else if (norm == 1) {
   	  sum=1.0/sum;
-          for (j=ptr[i]; j<ptr[i+1]; j++)
+  	}
+        for (j=ptr[i]; j<ptr[i+1]; j++)
             val[j] *= sum;

         }
@@ -1368,9 +1369,10 @@ void gk_csr_Normalize(gk_csr_t *mat, int what, int norm)
         if (sum > 0) {
   	if (norm == 2)
   	  sum=1.0/sqrt(sum);
-  	else if (norm == 1)
+  	else if (norm == 1) {
   	  sum=1.0/sum;
-          for (j=ptr[i]; j<ptr[i+1]; j++)
+  	}
+        for (j=ptr[i]; j<ptr[i+1]; j++)
             val[j] *= sum;
         }
       }
