--- a/CPP_INTERFACE/CPPWrapper.cpp
+++ b/CPP_INTERFACE/CPPWrapper.cpp
@@ -32,6 +32,18 @@
 #include <cassert>
 #include <complex>
 #include <iomanip>
+#include <climits>
+#include <unistd.h>
+
+// macOS / BSD libc does not define HOST_NAME_MAX (used by
+// bpack_write_pids_to_file below); fall back to _POSIX_HOST_NAME_MAX or 256.
+#ifndef HOST_NAME_MAX
+#  if defined(_POSIX_HOST_NAME_MAX)
+#    define HOST_NAME_MAX _POSIX_HOST_NAME_MAX
+#  else
+#    define HOST_NAME_MAX 256
+#  endif
+#endif


 #define OMPI_SKIP_MPICXX 1
