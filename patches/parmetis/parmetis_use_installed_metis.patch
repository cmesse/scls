--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,14 +1,8 @@
 cmake_minimum_required(VERSION 3.10)
 project(ParMETIS C)

-
-# Search for MPI.
-# GK commented this out as it seems to be creating problems
-# include(FindMPI)
-# if(NOT MPI_FOUND)
-#   message(FATAL_ERROR "mpi is not found")
-# endif()
-# set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${MPI_COMPILE_FLAGS}")
+# Find MPI (required for parallel partitioning)
+find_package(MPI REQUIRED)


 # Prepare libraries.
@@ -18,7 +12,10 @@
   set(ParMETIS_LIBRARY_TYPE STATIC)
 endif()

-include(./conf/gkbuild.cmake)
+# Set minimal GKlib-compatible flags without -march=native or -Werror
+# (our build system manages optimization flags via CFLAGS)
+set(GK_COPTIONS "-DLINUX -D_FILE_OFFSET_BITS=64 -std=c99 -fno-strict-aliasing -fPIC -DNDEBUG -DNDEBUG2")
+set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${GK_COPTIONS}")

 # List of paths that the compiler will search for header files.
 # i.e., the -I equivalent
--- a/libparmetis/CMakeLists.txt
+++ b/libparmetis/CMakeLists.txt
@@ -7,9 +7,7 @@
 # Create libparmetis
 add_library(parmetis ${ParMETIS_LIBRARY_TYPE} ${parmetis_sources})

-if(SHARED)
-  target_link_libraries(parmetis metis GKlib)
-endif()
+target_link_libraries(parmetis metis GKlib ${MPI_C_LIBRARIES})
  
 install(TARGETS parmetis
   LIBRARY DESTINATION lib
